#include "main.h"
/**
*print_last_digit - Entry point
* Description: 'last digit'
* @n: Entered data
* Return: always 0 (success)
*/
int print_last_digit(int n)
{
	int ld;

	ld = c % 10;
	printf("%d", ld);
	return (0);
}
